# 01 Generics

Focus on type parameters, bounded wildcards, invariance, covariance, and reusable generic APIs.

- `example/`: sample code for generic classes and methods
- `exercise/`: practice tasks for designing type-safe abstractions
